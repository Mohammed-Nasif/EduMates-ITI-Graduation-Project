	"homepage": "http://Mohammed-Nasif.github.io/EduMates-ITI-Graduation-Project",
	"predeploy": "npm run build",
		"deploy": "gh-pages -d build",
		"start": "set HTTPS=true && set PORT=2798 && react-scripts start",
		"build": "react-scripts build",
		"test": "react-scripts test",
		"eject": "react-scripts eject",
		"serve-json": "json-server --watch ./src/database/db.json --port=4200 --delay 800",
		"startdev": "concurrently --kill-others \"npm start\" \"npm run serve-json\""